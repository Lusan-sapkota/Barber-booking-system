POSTGRES_HOST = "localhost"
POSTGRES_PORT = 5432
POSTGRES_USER = "your_username"
POSTGRES_PASSWORD = "your_password"
POSTGRES_DB = "your_database"

#email settings
SMTP_SERVER = "smtp.example.com"
SMTP_PORT = 587
EMAIL_ADDRESS = ""
EMAIL_PASSWORD = ""

# Flask settings
FLASK_SECRET = "your_secret_key"
FLASK_DEBUG = True

# =========================================
# SAMPLE .env FILE - IMPORTANT NOTES
# =========================================

# 1. Replace the placeholder values with your actual configuration.
# 2. Do NOT commit this file to version control (e.g., GitHub).
# 3. Keep this file secure to avoid exposing sensitive information.
# 4. Use a library like python-dotenv to load these variables safely into your app.
# 5. After configuring, remove any default secret keys like:
#    app.secret_key = os.environ.get('FLASK_SECRET', 'your-default-secret-key')
#
#    Replace 'your-default-secret-key' with a secure value in your .env file.
#
# Example usage in your Flask app:
# app.secret_key = os.environ.get('FLASK_SECRET')
